<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.answertest"
    android:versionCode="2"
    android:versionName="1.1">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/com_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadDataActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnalogyExaminationActivity" />
        <activity android:name=".ActivityResultPage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ChapterActivity" />
        <activity android:name=".InformationInputActivity" />
        <activity android:name=".AnalogyQandAActivity"></activity>
    </application>

</manifest>